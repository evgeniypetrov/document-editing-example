h3 projects
ul.menu
  - for project in @projects
    - class_name = if project._id == @selected_id then "selected" else ""
    li(class="#{class_name}")
      a(href='#' data-id='#{ project._id }') #{ project.name }
