h1 hello world
div
  | not loaded yet?

javascript:
  bootstrap_data = {
    current_user: #{{ UserSerializer.new(current_user).to_json}}
  };
  $(document).ready(function() {
    window.App.start(bootstrap_data);
  });
